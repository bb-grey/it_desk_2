name: it_desk2
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  preload_page_view: ^0.1.6
  font_awesome_flutter: ^9.0.0
  syncfusion_flutter_pdfviewer: ^19.1.64-beta
  intl: ^0.17.0
  flutter_staggered_grid_view: ^0.4.0
  cupertino_icons: ^1.0.2
  firebase_core: ^1.2.0
  cloud_firestore: ^2.2.0
  firebase_auth: ^1.4.1
  firebase_storage: ^8.1.3
  path_provider: ^2.0.2
  firebase_app_check: ^0.0.1+2

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/splash/
    - assets/images/home/
    - assets/images/category/

    - assets/topics/database/
    - assets/topics/networking/
    - assets/topics/os/
    - assets/topics/software/
    - assets/topics/virus/

  fonts:
    - family: Lobster
      fonts:
        - asset: assets/fonts/Lobster-Regular.ttf
